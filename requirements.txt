# Runtime dependencies for Glove Shift
# Works on Python 3.10–3.14 (MediaPipe Tasks API; not the old solutions package)

mediapipe>=0.10.30
numpy>=1.26
PyQt6>=6.6

# OpenCV comes in via mediapipe (opencv-contrib-python).
# Do not also pin opencv-python — the two packages conflict.

# Optional packaging tools (see README-BUILD.md):
# pyinstaller>=6.0
